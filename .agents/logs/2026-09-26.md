# AI-assisted work log — 2026-09-26

## 14:28:23 +1000 — Issue #45

- **Prompt author:** tingyueh
- **Request:** Create a new branch and complete issues #45, #46, and #47,
  with one issue comment and one separate commit per issue. Start with WebRTC
  signaling and a DataChannel for devices on the same LAN.
- **Work completed:** Created the isolated
  `tingyueh/feat/webrtc-connectivity` worktree branch; documented the
  three-issue implementation plan; added authenticated newline-delimited TCP
  signaling; added host/guest SDP and host-candidate ICE exchange; adapted an
  ordered WebRTC DataChannel to the existing `DuelChannel` contract; added
  native TCP/WebRTC host and guest entry points, cancellation and cleanup;
  configured Expo local-network permission and the WebRTC config plugin; and
  added focused protocol, adapter, authorization, cleanup, and end-to-end
  in-memory negotiation tests.
- **AI model:** GPT-5 via Codex

## 14:53:26 +1000 — Issue #46

- **Prompt author:** tingyueh
- **Request:** Complete the shared-Wi-Fi QR connection flow as issue #46,
  keeping it separate from hotspot fallback and committing it independently.
- **Work completed:** Replaced the BLE-only invite shape with an authenticated
  WebRTC connection payload; added strict IPv4 and signaling-port validation;
  added Expo network discovery and network-change regeneration; started the
  host signaling server before presenting a QR code; forwarded connection and
  authentication fields through scanner confirmation and round selection; and
  connected the guest screen through the native WebRTC transport. Added and
  updated tests for QR validation, unusable Wi-Fi states, handoff completeness,
  and challenge-request serialization.
- **AI model:** GPT-5 via Codex
