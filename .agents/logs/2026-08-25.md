# AI-assisted work log — 2026-08-25

## 2026-08-25T12:37:11+10:00 — Fix MegaLinter REPOSITORY_TRIVY vulnerability error

**Author of prompt:** MRDGH2821  
**AI model:** Gemini 3.7 Flash High via Antigravity

**Prompt / request:**  
`/megalinter-fix fix the trivy error in this. I have fixed pretty much everything else`

**What was done:**

- Inspected Trivy error report (`megalinter-reports/linters_logs/REPOSITORY_TRIVY-ERROR.log`), identifying a high-severity vulnerability `CVE-2026-73646` in transitive dependency `postcss@8.4.49` (fixed in `>=8.5.18`).
- Added npm `overrides` in `package.json` for `"postcss": "^8.5.26"`.
- Ran `npm install` to update `package-lock.json` with `postcss@8.5.26`.
- Verified the fix by running MegaLinter `REPOSITORY_TRIVY` via container engine (`npx mega-linter-runner --linter REPOSITORY_TRIVY --container-engine podman`), which passed with 0 errors.
- Ran project formatters and linters (`mise run fmt`, `mise run check`, `mise run cspell`), ensuring clean tree.

## 2026-08-25T13:32:30+10:00 — Fix CI Prettier failure on lockfiles

**Author of prompt:** MRDGH2821  
**AI model:** Gemini 3.7 Flash High via Antigravity

**Prompt / request:**  
`for some reason this CI keeps failing, despite locally working fine  https://github.com/pix-l-crafters/pocket-draw/actions/runs/32803826335/job/97669898795`

**What was done:**

- Analyzed GitHub Actions run `32803826335` logs, identifying `hk check --all` failing during `prettier --check` on `apm.lock.yaml` and `package-lock.json`.
- Root cause: In CI, `mise-action` runs `apm install` (which creates `apm.lock.yaml`) and `mise run prepare` runs `npm install` (which updates `package-lock.json` with npm's native serializer). Prettier then ran on these generated lockfiles and flagged formatting mismatches.
- Created `.prettierignore` ignoring lockfiles (`package-lock.json`, `*.lock`, `apm.lock.yaml`, `*.lock.yaml`) and build/cache folders.
- Updated `.treefmt.toml` to exclude `package-lock.json` and `apm.lock.yaml` from Prettier formatting.
- Verified that `mise run prepare && mise run check && mise run cspell` passes cleanly locally.
