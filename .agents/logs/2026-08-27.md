# AI-assisted work log — 2026-08-27

## 2026-08-27T07:05:05+10:00 — Fix Android BLE build

**Author of prompt:** MRDGH2821  
**AI model:** Claude Sonnet 5 via Claude Code

**Prompt / request:**  
`can you make this work on android` (BLE feature on the `bluetooth-integration` branch), followed by `do this work in a new branch`

**What was done:**

- Diagnosed and fixed three chained Android native build failures on branch `mihir/fix/android-ble`:
  1. `adb` missing from the mise-managed Android SDK (`platform-tools` component wasn't installed) — installed it via `sdkmanager`.
  2. Gradle 8.14.3 failing with `Unsupported class file major version 69` because the system default JDK was 25 — pinned `java = "21"` in `mise.toml`.
  3. `expo-modules-core:compileDebugKotlin` failing on `Promise.kt` due to an ABI mismatch:
     `package.json` had `react-native@0.86.2`/`react@19.2.3`/`expo-status-bar@~57.0.1`
     (versions matching Expo SDK 57) while `expo` itself was pinned to SDK 54, which bundles
     `react-native@0.81.5`. Ran `npx expo install --fix` to realign dependency versions to what
     Expo SDK 54 expects, then regenerated the native `android/` project via
     `expo prebuild --platform android --clean`.
- Verified the app builds, installs, and runs on a physical Android device, with BLE scanning confirmed working by the user.
- Confirmed with the user that getting onto newer dependencies (e.g. `react-native@0.86.x`) requires bumping the whole Expo SDK (54 → 57) together via `expo install expo@57 --fix`, not cherry-picking individual packages — deferred as separate follow-up work since it's a 3-major-version SDK jump affecting both platforms.
