# AI-assisted work log — 2026-09-09

## 2026-09-09T01:13:34+10:00 — Set a real iOS bundle identifier and document local code signing

**Author of prompt:** sihengma
**AI model:** Claude Sonnet 5 via Claude Code

**Prompt / request:**

Walked through first-time local setup to run the app on a physical iPhone
(`npx expo run:ios --device`). Hit two blockers worth recording:

- `npx expo run:ios` failed with `No iOS devices available in Simulator.app`
  even with the device connected and detected — Expo CLI's device resolution
  calls `getBestSimulatorAsync` before falling back to a physical device and
  throws when zero iOS simulator runtimes are installed. Fixed by installing
  one runtime (`xcodebuild -downloadPlatform iOS`).
- `CommandError: No code signing certificates are available` — no Apple
  Development certificate existed on the machine. Resolved by configuring
  automatic signing in Xcode with a free personal team, which required
  changing the placeholder bundle id.

**What was done:**

- Changed `expo.ios.bundleIdentifier` in [`app.json`](../../app.json) from
  `com.anonymous.pocketdraw` to `com.sihengm.pocketdraw` so it matches what was
  registered for signing and so `expo prebuild` regenerates `ios/` consistently.
  Android `package` left as-is (debug keystore, no registration constraint).
- Added a "Code signing" note and a simulator-runtime troubleshooting note to
  [`README.md`](../../README.md): `ios/`/`android/` are git-ignored, so each
  contributor configures their own signing team locally and, if the shared
  bundle id is unavailable to their free Apple account, overrides it locally to
  `com.<name>.pocketdraw` without affecting anyone else.

**Verification:** app `com.sihengm.pocketdraw` built and installed on the
physical device from Xcode; terminal `npx expo run:ios --device <udid>` reaches
the signing/build step.
