# AI-assisted work log — 2026-09-10

## 2026-09-10T17:33:03+10:00 — Iteration: fetch and display other players' locations

**Author of prompt:** Ethan
**AI model:** Claude Sonnet 5 via Claude Code

**Prompt / request:**

Split the map work into iterations. This iteration: let a user see other
users' locations on the map (read presence from Firestore), replacing the
hardcoded `MOCK_PLAYERS`. Firestore rules were deployed by the author before
this work. Out of scope: continuous/periodic self-publishing, distance
filtering, marker interaction.

**What was done:**

- [`map.types.ts`](../../src/features/map/types/map.types.ts): added `NearbyPlayer`
  and a `NearbyPlayersState` discriminated union (loading / ready / error),
  matching the existing `LocationState` / `PresencePublishState` style.
- [`map.constants.ts`](../../src/features/map/constants/map.constants.ts): added
  `PRESENCE_STALE_AFTER_MS` (10 min, generous until continuous publishing lands)
  and a `PLAYER_PIN_COLORS` palette.
- [`map.utils.ts`](../../src/features/map/utils/map.utils.ts): added
  `pinColorForUid` — deterministic marker colour from a uid hash.
- [`presenceRepository.ts`](../../src/features/map/services/presenceRepository.ts):
  added `subscribeToVisiblePresence({ onData, onError })` — an `onSnapshot`
  listener on `query(collection(db, "presence"), where("isVisible", "==", true))`.
  Docs are parsed defensively; entries with missing/mistyped fields or a
  still-pending `serverTimestamp` (`lastSeen == null`) are skipped.
- [`useNearbyPlayers.ts`](../../src/features/map/hooks/useNearbyPlayers.ts): new
  hook that owns the subscription lifecycle and filters out the current user and
  stale entries.
- [`MapScreen.tsx`](../../src/features/map/MapScreen.tsx): removed `MOCK_PLAYERS`,
  renders markers from `useNearbyPlayers`, colours them via `pinColorForUid`.
- [`MapStatusCard.tsx`](../../src/features/map/components/MapStatusCard.tsx): headline
  now reflects the nearby-players state ("N players nearby" / "No players nearby" /
  "Couldn't load nearby players").

**Notes / follow-ups:**

- The `where("isVisible", "==", true)` filter is required — the read rule only
  permits other users' docs when `resource.data.isVisible == true`, so an
  unfiltered query would be rejected wholesale.
- Staleness is only re-evaluated when a new snapshot arrives; a player who stops
  publishing lingers until the next snapshot. Acceptable until the continuous
  self-publish iteration tightens the window.

**Verification:** `npx tsc --noEmit` clean; `npx prettier --check` clean. Not yet
run against two live devices.
