# AI-assisted work log — 2026-09-10

## 2026-09-10T17:33:03+10:00 — Iteration: fetch and display other players' locations

**Author of prompt:** Ethan
**AI model:** Claude Sonnet 5 via Claude Code

**Prompt / request:**

Split the map work into iterations. This iteration: let a user see other
users' locations on the map (read presence from Firestore), replacing the
hardcoded `MOCK_PLAYERS`. Firestore rules were deployed by the author before
this work. Out of scope: continuous/periodic self-publishing, distance
filtering, marker interaction.

**What was done:**

- [`map.types.ts`](../../src/features/map/types/map.types.ts): added `NearbyPlayer`
  and a `NearbyPlayersState` discriminated union (loading / ready / error),
  matching the existing `LocationState` / `PresencePublishState` style.
- [`map.constants.ts`](../../src/features/map/constants/map.constants.ts): added
  `PRESENCE_STALE_AFTER_MS` (10 min, generous until continuous publishing lands)
  and a `PLAYER_PIN_COLORS` palette.
- [`map.utils.ts`](../../src/features/map/utils/map.utils.ts): added
  `pinColorForUid` — deterministic marker colour from a uid hash.
- [`presenceRepository.ts`](../../src/features/map/services/presenceRepository.ts):
  added `subscribeToVisiblePresence({ onData, onError })` — an `onSnapshot`
  listener on `query(collection(db, "presence"), where("isVisible", "==", true))`.
  Docs are parsed defensively; entries with missing/mistyped fields or a
  still-pending `serverTimestamp` (`lastSeen == null`) are skipped.
- [`useNearbyPlayers.ts`](../../src/features/map/hooks/useNearbyPlayers.ts): new
  hook that owns the subscription lifecycle and filters out the current user and
  stale entries.
- [`MapScreen.tsx`](../../src/features/map/MapScreen.tsx): removed `MOCK_PLAYERS`,
  renders markers from `useNearbyPlayers`, colours them via `pinColorForUid`.
- [`MapStatusCard.tsx`](../../src/features/map/components/MapStatusCard.tsx): headline
  now reflects the nearby-players state ("N players nearby" / "No players nearby" /
  "Couldn't load nearby players").

**Notes / follow-ups:**

- The `where("isVisible", "==", true)` filter is required — the read rule only
  permits other users' docs when `resource.data.isVisible == true`, so an
  unfiltered query would be rejected wholesale.
- Staleness is only re-evaluated when a new snapshot arrives; a player who stops
  publishing lingers until the next snapshot. Acceptable until the continuous
  self-publish iteration tightens the window.

**Verification:** `npx tsc --noEmit` clean; `npx prettier --check` clean. Not yet
run against two live devices.

## 2026-09-10T18:20:00+10:00 — Iteration: continuous location publishing

**Author of prompt:** Ethan
**AI model:** Claude Sonnet 5 via Claude Code

**Prompt / request:**

Next iteration: publish the current user's location continuously so other
players' pins actually move, instead of the one-shot write per map visit.
Fold in the cold-start `permission-denied` retry fix noted earlier.

**What was done:**

- [`useForegroundLocation.ts`](../../src/features/map/hooks/useForegroundLocation.ts):
  after the initial fix, starts a `watchPositionAsync` subscription
  (`distanceInterval` = `LOCATION_WATCH_DISTANCE_M`, 10 m) and pushes each update
  into `locationState`. Subscription is torn down on unmount and on retry.
- [`usePresencePublisher.ts`](../../src/features/map/hooks/usePresencePublisher.ts):
  rewritten around a self-scheduling `publish()` —
  - keyed on the **coarsened** cell, so GPS jitter inside one ~110 m cell does
    not cause writes;
  - **heartbeat**: re-publishes every `PRESENCE_HEARTBEAT_MS` (60 s) to keep
    `lastSeen` fresh;
  - **retry with backoff**: 2s / 4s / 8s, staying in `publishing` until the
    retries are exhausted, only then `error` (fixes the cold-start
    auth-token/connection race);
  - a second effect deletes the presence doc on unmount / logout (best effort —
    see follow-up).
- [`presenceRepository.ts`](../../src/features/map/services/presenceRepository.ts):
  added `removePresence(uid)` (a `deleteDoc` — rules forbid writing
  `isVisible: false`).
- [`useNearbyPlayers.ts`](../../src/features/map/hooks/useNearbyPlayers.ts):
  re-checks staleness on a timer, not only when a snapshot arrives, so silent
  players drop off.
- [`map.constants.ts`](../../src/features/map/constants/map.constants.ts):
  `LOCATION_WATCH_DISTANCE_M`, `PRESENCE_HEARTBEAT_MS`,
  `PRESENCE_MAX_PUBLISH_RETRIES`, `PRESENCE_RETRY_BASE_DELAY_MS`;
  `PRESENCE_STALE_AFTER_MS` tightened from 10 min to 3 × heartbeat (3 min).
- [`tsconfig.json`](../../tsconfig.json): `exclude` `apm_modules` / `node_modules`
  — `apm install` pulled skill scripts with Bun/Node globals that were breaking
  `tsc`.

**Follow-ups:**

- Logout deletes presence only best-effort: `signOut` runs first, so the delete
  usually hits the rules unauthenticated and fails; the doc then ages out via
  the 3 min staleness window. A clean fix removes presence before `signOut` in
  `App.tsx`.
- No background-location handling; presence only updates while the map screen is
  foregrounded.
- Manual privacy/invisible toggle (task 2.4) still outstanding — `removePresence`
  is the building block.

**Verification:** `npx tsc --noEmit` clean; `npx prettier --check` clean. Not yet
run against two live devices.

## 2026-09-10T19:05:00+10:00 — Task 2.4: privacy / invisible toggle

**Author of prompt:** Ethan
**AI model:** Claude Sonnet 5 via Claude Code

**Prompt / request:**

Backlog 2.4 — let the player turn location sharing off from the map, persisted
across launches. Builds on `removePresence` from the previous entry.

**What was done:**

- [`useSharingPreference.ts`](../../src/features/map/hooks/useSharingPreference.ts):
  new hook backing an `isSharing` boolean with AsyncStorage
  (`pocket-draw:location-sharing-enabled`, default on). `isSharing` is `null`
  until the stored value is read so the publisher does not act on a guess.
- [`usePresencePublisher.ts`](../../src/features/map/hooks/usePresencePublisher.ts):
  new `enabled: boolean | null` input. `null` → no-op; `false` → set state
  `"off"` and `removePresence` once (guarded by a ref so moving while hidden
  does not re-delete); `true` → publish/heartbeat as before.
- [`map.types.ts`](../../src/features/map/types/map.types.ts): `PresencePublishState`
  gains `{ status: "off" }`.
- [`SharingToggle.tsx`](../../src/features/map/components/SharingToggle.tsx): new —
  a Paper `Chip` (eye / eye-off, "Sharing" / "Hidden") in the map overlay,
  hidden until the preference loads.
- [`MapStatusCard.tsx`](../../src/features/map/components/MapStatusCard.tsx): shows
  "Location sharing is off" when presence is off.
- [`MapScreen.tsx`](../../src/features/map/MapScreen.tsx): wires the hook, passes
  `enabled` to the publisher, renders `SharingToggle` under the status card.

**Notes:**

- Turning off deletes the presence doc, so other players lose the pin
  immediately (faster than the staleness window).
- The local "You" marker still shows when hidden — only the broadcast stops.

**Verification:** `npx tsc --noEmit` clean; `npx prettier --check` clean. Not yet
run against two live devices.

## 2026-09-10T19:45:00+10:00 — Task 2.2: player pin popup (name / wins / ELO)

**Author of prompt:** Ethan
**AI model:** Claude Sonnet 5 via Claude Code

**Prompt / request:**

Backlog 2.2 — tapping another player's map pin shows their name, wins and ELO.
Stub the stats against `src/contracts/mocks/mockPlayerStats.ts` until Mihir's
backend (5.1/5.4/5.5) lands.

**What was done:**

- [`usePlayerStats.ts`](../../src/features/map/hooks/usePlayerStats.ts): new hook,
  returns `PlayerStats | null` for a selected player. Wraps `mockPlayerStats` in
  `Promise.resolve` so swapping in the real async lookup later is a one-line
  change; carries the `TODO` to delete the mock import.
- [`PlayerStatsCard.tsx`](../../src/features/map/components/PlayerStatsCard.tsx):
  new — a Paper `Surface` at the bottom of the map showing the name
  (`DisplayHeading` + `KickerLabel`), three `StatTile`s (Wins / Losses / ELO),
  and a close button. Renders "—" per stat until data arrives.
- [`PlayerMarker.tsx`](../../src/features/map/components/PlayerMarker.tsx): added
  `onPress`; when set, the native callout (title/description) is suppressed so
  the custom card is the only popup.
- [`MapScreen.tsx`](../../src/features/map/MapScreen.tsx): tracks
  `selectedPlayerUid`; tapping a nearby pin selects it, tapping the map (guarded
  on `action !== "marker-press"`) or the card's close button clears it. The
  stats card replaces the recenter FAB while a player is selected. The "You"
  marker keeps its plain native callout.

**Notes:**

- `mockPlayerStats` returns the same numbers for every uid — expected for the
  mock.
- If a selected player goes offline the card disappears (they drop out of
  `nearbyPlayers`).
- Rare overlap: the stats card (`bottom: 24`) and `LocationStatusCard`
  (`bottom: 84`) can stack if your own location is denied while you inspect a
  peer. Both are dismissible; left for a later polish pass.

**Verification:** `npx tsc --noEmit` clean; `npx prettier --check` clean. Not yet
run against two live devices.

## 17:52 — Rebase QR payload/validation onto design-system baseline

**Prompt author:** tingyueh

**Prompt:** Read AGENTS.md, the task-split docs, and the contracts README; confirm the base branch to start from; create the `tingyue/feat/qr-challenge` branch; rebase the orphaned QR payload/validation work (`feature/payload-rebased`) onto it; work through the QR challenge ticket list, using contract mocks where needed and stopping to ask on open design decisions.

**What was done:**

- Confirmed with the user to branch from `dev` (0341114) despite it being flagged stale in the task-split docs, since the design-system/EAS/map-duel branches don't share a common integrated tip yet.
- Created `tingyue/feat/qr-challenge` off `dev`.
- Brought over `src/features/qr/types/qr.types.ts` and `src/features/qr/utils/qr.validation.ts` from `feature/payload-rebased`.
- Cleaned up both files: removed module-scope `console.log` scratch/debug
  blocks (ran on every import), fixed a broken example in
  `qr.validation.ts` that referenced `MAX_QR_PAYLOAD_LENGTH` outside its
  declaring function scope, hoisted magic numbers
  (`MAX_QR_PAYLOAD_LENGTH`, `INVITE_LIFETIME_MS`, `MAX_CLOCK_SKEW_MS`)
  into named constants, and translated inline Chinese comments to
  English for consistency with the rest of the codebase.
- Verified with `tsc --noEmit` and `hk run fix` (prettier, cspell, typos, betterleaks, ls-lint, keep-sorted all pass).

**Model:** Claude Sonnet 5

## 17:59 — Implement 3.1-3.3: QR display, scanner, opponent popup

**Prompt author:** tingyueh

**Prompt:** Confirmed dev already has the design-system baseline (theme
tokens + shared components), so continue with tickets 3.1-3.3 on top of
the rebased QR payload/validation work.

**What was done:**

- Added `expo-camera`, `expo-crypto`, `react-native-svg`, and
  `react-native-qrcode-svg` via `npx expo install` (SDK-54-compatible
  versions), plus the `expo-camera` config plugin in `app.json` for the
  camera permission description.
- `src/features/qr/utils/qr.tokens.ts`: token/UUID generation backed by
  `expo-crypto` (`getRandomBytes`, `randomUUID`), matching the formats
  `qr.validation.ts` already expects.
- `src/features/challenge/QrDisplayScreen.tsx` (3.1): generates and
  renders a player's invite QR code using the shared design-system
  components, auto-regenerating on the invite's one-minute expiry.
- `src/features/challenge/QrScannerScreen.tsx` (3.2): camera-based
  scanner (`expo-camera`'s `CameraView`) that parses scanned data with
  `parseQrInvite` and surfaces validation errors inline.
- `src/features/challenge/components/OpponentPopup.tsx` (3.3): modal
  showing the scanned opponent's name/wins/losses/ELO, built against
  `src/contracts/playerStats.ts` and its mock (Mihir's 5.1/5.4/5.5
  backend isn't ready yet) rather than blocking on it.
- `src/features/challenge/ChallengeScreen.tsx`: container screen
  toggling between "My QR" and "Scan", wired into `App.tsx` as a new
  "Challenge" tab.
- On confirming a scanned opponent, `QrScannerScreen` builds a
  `ChallengeHandoff` (`src/contracts/challengeHandoff.ts`) — this is the
  producer side of that contract; Siheng's 3.4-3.6/3.8 can consume it
  once it's ready. `App.tsx` currently just logs the handoff since that
  consumer doesn't exist yet.
- Verified with `tsc --noEmit` (no new errors; three pre-existing
  `apm_modules` errors are unrelated and present on `dev` too) and
  `hk run fix` (all checks pass, including `cspell` after adding
  "qrcode" to the dictionary).

**Model:** Claude Sonnet 5

## 18:20:40+10:00 — Match results backend (5.1–5.3)

**Author:** MRDGH2821 (Mihir)

**Prompt / request:** Follow `prompt.md`: work Mihir’s task-split tickets from `origin/dev`; use Hermes and Agy via Herdr for parallel work.

**Model:** Composer via Cursor (orchestration); Hermes (`solar-pro4:free`) assisted on Firestore rules; Agy started then stalled (credits / Memorix permission prompt) — offline queue implemented by Cursor instead.

**Branch:** `mihir/feat/match-results-backend` (from `origin/dev` @ `34bcb0d`)

**What was done:**

- Created `src/features/backend/` match-results write path, offline AsyncStorage queue, reconnect flush helpers, and `playerStatsRepository` (wins/losses from `matchResults`; ELO default until Tanachat 5.4).
- Extended `firestore.rules` for immutable `matchResults/{matchId}` creates (Hermes draft; fixed create-path `resource` bugs so validation uses path `matchId` + `request.resource.data`).
- Noted consumers still on `mockPlayerStats` until they swap to `playerStatsRepository`.

**Not done (by design / blocked):**

- 7.1 / 7.3 device QA — wait until full loop is demoable.
- Tooling reconcile (`mihir/feat/secrets-setup-rebased`) — separate chore branch.
- `@react-native-community/netinfo` not installed; connectivity is optimistic + enqueue on Firestore network errors.

## AI-assisted work log

### 2026-09-10T18:00:00+10:00 — Tanachat pre-round ritual

- Prompt author: hbeat
- Request: Build Tanachat's assigned backlog tickets from the confirmed `dev` starting point, using shared contracts and mocks where needed.
- Work completed: Created `tanachat/feat/duel-pre-round-ritual`, completed the required APM/mise setup, recorded the BLE spike as incomplete (scanner-only; no host/peripheral flow), and added the pre-round ritual foundation using `DuelChannel` plus accelerometer face-down detection, RSSI separation helpers, randomized buzz, and synchronized countdown messages.
- Decisions applied: BLE RSSI proxy for separation; the buzz is part of the countdown sequence.
- Deferred: ELO implementation pending starting rating and K-factor values.
- AI model: GPT-5 via Codex.

### 2026-09-10T19:30:00+10:00 — Local countdown audio placeholder

- Prompt author: hbeat
- Request: Use `expo-audio`, play countdown audio locally, and leave the asset for later.
- Work completed: Added `expo-audio`, a replaceable `COUNTDOWN_AUDIO_SOURCE` placeholder, and local playback calls for each countdown tick. The BLE channel does not carry audio.
- AI model: GPT-5 via Codex.

### 2026-09-10T19:10:00+10:00 — Haptic feedback and channel injection

- Prompt author: hbeat
- Request: Complete all remaining assigned work.
- Work completed: Added native Expo haptic feedback for the buzz and countdown ticks, and allowed `DuelScreen` to receive the real `DuelChannel` and RSSI reader when the BLE producer is available.
- Remaining external validation: audio assets/playback, real BLE producer wiring, and two-device Android/iPhone testing require the integrated team branch and physical devices.
- AI model: GPT-5 via Codex.

### 2026-09-10T18:30:00+10:00 — Live RSSI handoff boundary

- Prompt author: hbeat
- Request: Complete remaining items 2, 3, and 4.
- Work completed: Added a `react-native-ble-manager` RSSI reader adapter and made the Duel screen accept it without coupling the pre-round component to BLE internals.
- External blockers: Siheng's real `DuelChannel` is not present on any remote branch, so live channel wiring and two-device validation remain pending. Mihir's backend branch is also not integrated and still contains the placeholder ELO rating; this branch does not overwrite another producer's backend implementation.
- AI model: GPT-5 via Codex.

### 2026-09-10T18:15:00+10:00 — Finish pre-round integration

- Prompt author: hbeat
- Request: Continue and finish the remaining assigned work after confirming ELO rating 1500 and K-factor 40.
- Work completed: Added the standard ELO update utility, exposed the pre-round ritual through a Duel screen using the shared loopback mock, and added Duel navigation to the authenticated app shell.
- Integration note: The Duel screen remains mock-backed until Siheng's real `DuelChannel` producer replaces the mock and supplies live RSSI readings.
- AI model: GPT-5 via Codex.

### 2026-09-10 — AI-assisted work log

#### 18:20:40+10:00 — Match results backend (5.1–5.3)

**Author:** MRDGH2821 (Mihir)

**Prompt / request:** Follow `prompt.md`: work Mihir’s task-split tickets from `origin/dev`; use Hermes and Agy via Herdr for parallel work.

**Model:** Composer via Cursor (orchestration); Hermes (`solar-pro4:free`) assisted on Firestore rules; Agy started then stalled (credits / Memorix permission prompt) — offline queue implemented by Cursor instead.

**Branch:** `mihir/feat/match-results-backend` (from `origin/dev` @ `34bcb0d`)

**What was done:**

- Created `src/features/backend/` match-results write path, offline AsyncStorage queue, reconnect flush helpers, and `playerStatsRepository` (wins/losses from `matchResults`; ELO default until Tanachat 5.4).
- Extended `firestore.rules` for immutable `matchResults/{matchId}` creates (Hermes draft; fixed create-path `resource` bugs so validation uses path `matchId` + `request.resource.data`).
- Noted consumers still on `mockPlayerStats` until they swap to `playerStatsRepository`.

**Not done (by design / blocked):**

- 7.1 / 7.3 device QA — wait until full loop is demoable.
- Tooling reconcile (`mihir/feat/secrets-setup-rebased`) — separate chore branch.
- `@react-native-community/netinfo` not installed; connectivity is optimistic + enqueue on Firestore network errors.

### 2026-09-10T18:50:00+10:00 — ELO in Firestore player stats

- Prompt author: hbeat
- Request: Integrate ELO updates into Firestore/player stats.
- Work completed: Imported Mihir's match-results repository layer, set the shared starting rating to 1500, and updated `playerStatsRepository` to replay completed Firestore match results chronologically with K-factor 40 before returning the current ELO.
- Validation note: Project formatting/hooks pass. Full TypeScript validation still reports existing `apm_modules` JSX errors and Firebase environment typing errors.
- AI model: GPT-5 via Codex.

## Merged from tianze/feat/duel-fire-detection

## AI-assisted work log — 2026-09-10

### 2026-09-10T17:37:06+10:00 — Ticket 4.8 FIRE signal synchronization

- **Prompt author:** Tianze Wu
- **Request:** Start Tianze's assigned backlog from the confirmed `dev` base,
  follow the shared-contract workflow, and complete tickets in order without
  guessing at open design decisions.
- **Work completed:** Created `tianze/feat/duel-fire-detection` from `dev`. Added
  a host-authoritative FIRE signal coordinator that gates firing on countdown
  completion and connection state, synchronizes the host timestamp through
  `DuelChannel`, exposes the signal to both players, and rejects guest,
  premature, or duplicate triggers. Added a local two-player helper backed by
  `mockDuelChannel` so the flow remains testable before the BLE implementation
  lands. Marked ticket 4.8 complete in Tianze's task split.
- **Validation:** Passed a strict scoped TypeScript check and a compiled
  mock-channel smoke test covering both-player synchronization and trigger
  guards. The repository-wide TypeScript check remains blocked by cached APM
  Expo skill scripts that reference missing Bun types outside the app source.
- **AI model:** GPT-5 via Codex

### 2026-09-10T18:08:09+10:00 — Merge latest dev

- **Prompt author:** Tianze Wu
- **Request:** Merge `dev` into `tianze/feat/duel-fire-detection`.
- **Work completed:** Fetched `origin/dev`, confirmed local `dev` matched it at
  `34bcb0d`, and merged it into the feature branch. The merge applied cleanly
  without conflict markers and brought in the latest shared environment
  configuration.
- **AI model:** GPT-5 via Codex

### 2026-09-10T18:35:05+10:00 — Ticket 4.9 raise gesture specification

- **Prompt author:** Tianze Wu
- **Request:** Continue all assigned tasks using the confirmed gesture,
  false-start, tie, and reconnect decisions, then open a pull request.
- **Work completed:** Recorded the approved design decisions in Tianze's task
  split and added a typed raise gesture specification: 1.8g resultant
  acceleration sustained for 80ms with a 300ms debounce. Marked ticket 4.9
  complete.
- **AI model:** GPT-5 via Codex

### 2026-09-10T18:40:47+10:00 — Ticket 4.10 accelerometer detection

- **Prompt author:** Tianze Wu
- **Request:** Implement the approved raise-to-threshold gesture detector.
- **Work completed:** Installed the Expo SDK-compatible `expo-sensors` module,
  configured Android high-rate sampling and the iOS motion permission message,
  added a pure sustained-threshold/debounce detector, and added an
  Accelerometer monitor that handles availability and permissions. Marked
  ticket 4.10 complete.
- **Validation:** Passed strict scoped TypeScript compilation, Expo public
  configuration resolution, and deterministic detector smoke tests covering
  threshold duration and debounce behavior.
- **AI model:** GPT-5 via Codex

### 2026-09-10T18:42:57+10:00 — Ticket 4.11 draw calibration

- **Prompt author:** Tianze Wu
- **Request:** Continue to the "Test your draw" calibration step.
- **Work completed:** Added a design-system-aligned calibration screen that
  checks sensor availability and permission, listens for one approved raise,
  reports pass and error states accessibly, supports retries, cleans up the
  native subscription, and exposes completion to the duel flow. Marked ticket
  4.11 complete.
- **AI model:** GPT-5 via Codex

### 2026-09-10T18:46:16+10:00 — Ticket 4.12 false-start detection

- **Prompt author:** Tianze Wu
- **Request:** Continue with false-start detection using the approved 0.25g
  movement threshold.
- **Work completed:** Added a pure detector that can be armed before the cue,
  compares resultant acceleration with resting gravity, emits one false-start
  event at a 0.25g deviation, and disarms at FIRE. Marked ticket 4.12 complete.
- **AI model:** GPT-5 via Codex

### 2026-09-10T18:49:26+10:00 — Ticket 4.13 false-start handling

- **Prompt author:** Tianze Wu
- **Request:** Apply the approved rule that a false start loses only the
  current round.
- **Work completed:** Added a coordinator that turns local early movement into
  the shared `RoundOutcome` false-start shape, sends the event through the
  `DuelChannel`, and resolves the same offending player on the peer. Added a
  mock-channel pair for local two-player testing and marked ticket 4.13
  complete.
- **AI model:** GPT-5 via Codex

### 2026-09-10T18:52:39+10:00 — Ticket 4.14 reaction timing

- **Prompt author:** Tianze Wu
- **Request:** Capture player reaction time in milliseconds.
- **Work completed:** Added a resettable one-shot reaction timer that records
  local FIRE and raise timestamps, produces elapsed milliseconds, ignores
  pre-FIRE readings, and prevents duplicate sensor events from changing the
  first valid result. Marked ticket 4.14 complete.
- **AI model:** GPT-5 via Codex

### 2026-09-10T18:55:36+10:00 — Ticket 4.15 tie handling

- **Prompt author:** Tianze Wu
- **Request:** Treat reactions within 100ms as a tie and award one point to
  each player.
- **Work completed:** Added the real round judge and scoring functions, using
  an inclusive 100ms tie window and one point per tied player. Extended the
  shared tie outcome with the opponent reaction and explicit tie points, and
  updated the mock to match. Marked ticket 4.15 complete.
- **Contract handoff:** Mobark and Tanachat must use the updated tie fields.
  Mobark can replace `mockRoundOutcome` with `resolveRoundOutcome` after this
  branch lands, then delete the mock as described in the contracts guide.
- **AI model:** GPT-5 via Codex

### 2026-09-10T18:58:57+10:00 — Ticket 4.18 disconnect recovery

- **Prompt author:** Tianze Wu
- **Request:** Retry a mid-round or mid-match disconnect automatically twice,
  then abort the match if recovery fails, allowing three seconds per attempt.
- **Work completed:** Added transport-independent disconnect recovery with two
  timeout-bound attempts, coalesced concurrent recovery requests, observable
  retry/recovered/aborted states, cancellation, and match-abort context.
  Updated the approved duel design and marked ticket 4.18 complete.
- **AI model:** GPT-5 via Codex

---

### 2026-09-10

#### 17:52 — Rebase QR payload/validation onto design-system baseline

**Prompt author:** tingyueh

**Prompt:** Read AGENTS.md, the task-split docs, and the contracts README; confirm the base branch to start from; create the `tingyue/feat/qr-challenge` branch; rebase the orphaned QR payload/validation work (`feature/payload-rebased`) onto it; work through the QR challenge ticket list, using contract mocks where needed and stopping to ask on open design decisions.

**What was done:**

- Confirmed with the user to branch from `dev` (0341114) despite it being flagged stale in the task-split docs, since the design-system/EAS/map-duel branches don't share a common integrated tip yet.
- Created `tingyue/feat/qr-challenge` off `dev`.
- Brought over `src/features/qr/types/qr.types.ts` and `src/features/qr/utils/qr.validation.ts` from `feature/payload-rebased`.
- Cleaned up both files: removed module-scope `console.log` scratch/debug
  blocks (ran on every import), fixed a broken example in
  `qr.validation.ts` that referenced `MAX_QR_PAYLOAD_LENGTH` outside its
  declaring function scope, hoisted magic numbers
  (`MAX_QR_PAYLOAD_LENGTH`, `INVITE_LIFETIME_MS`, `MAX_CLOCK_SKEW_MS`)
  into named constants, and translated inline Chinese comments to
  English for consistency with the rest of the codebase.
- Verified with `tsc --noEmit` and `hk run fix` (prettier, cspell, typos, betterleaks, ls-lint, keep-sorted all pass).

**Model:** Claude Sonnet 5

#### 17:59 — Implement 3.1-3.3: QR display, scanner, opponent popup

**Prompt author:** tingyueh

**Prompt:** Confirmed dev already has the design-system baseline (theme
tokens + shared components), so continue with tickets 3.1-3.3 on top of
the rebased QR payload/validation work.

**What was done:**

- Added `expo-camera`, `expo-crypto`, `react-native-svg`, and
  `react-native-qrcode-svg` via `npx expo install` (SDK-54-compatible
  versions), plus the `expo-camera` config plugin in `app.json` for the
  camera permission description.
- `src/features/qr/utils/qr.tokens.ts`: token/UUID generation backed by
  `expo-crypto` (`getRandomBytes`, `randomUUID`), matching the formats
  `qr.validation.ts` already expects.
- `src/features/challenge/QrDisplayScreen.tsx` (3.1): generates and
  renders a player's invite QR code using the shared design-system
  components, auto-regenerating on the invite's one-minute expiry.
- `src/features/challenge/QrScannerScreen.tsx` (3.2): camera-based
  scanner (`expo-camera`'s `CameraView`) that parses scanned data with
  `parseQrInvite` and surfaces validation errors inline.
- `src/features/challenge/components/OpponentPopup.tsx` (3.3): modal
  showing the scanned opponent's name/wins/losses/ELO, built against
  `src/contracts/playerStats.ts` and its mock (Mihir's 5.1/5.4/5.5
  backend isn't ready yet) rather than blocking on it.
- `src/features/challenge/ChallengeScreen.tsx`: container screen
  toggling between "My QR" and "Scan", wired into `App.tsx` as a new
  "Challenge" tab.
- On confirming a scanned opponent, `QrScannerScreen` builds a
  `ChallengeHandoff` (`src/contracts/challengeHandoff.ts`) — this is the
  producer side of that contract; Siheng's 3.4-3.6/3.8 can consume it
  once it's ready. `App.tsx` currently just logs the handoff since that
  consumer doesn't exist yet.
- Verified with `tsc --noEmit` (no new errors; three pre-existing
  `apm_modules` errors are unrelated and present on `dev` too) and
  `hk run fix` (all checks pass, including `cspell` after adding
  "qrcode" to the dictionary).

**Model:** Claude Sonnet 5

#### 2026-09-10T19:16:16+10:00 — Final integration and pull request scope

- **Prompt author:** Tianze Wu
- **Request:** Open a pull request for completed tickets 4.8–4.15 and 4.18;
  defer iOS signing/TestFlight (0.5) and the physical-device full-loop test
  (7.2) to later work.
- **Work completed:** Merged the latest `origin/dev` into
  `tianze/feat/duel-fire-detection`, preserving both the QR camera plugin and
  duel motion-sensor plugin plus both contributors' AI work logs. Kept tickets
  0.5 and 7.2 unchecked and scoped the pull request to the completed duel
  detection and round-resolution work.
- **AI model:** GPT-5 via Codex

## Merged from mobark/feat/postmatch-summary

## AI-assisted work log — 2026-09-10

### 2026-09-10T17:12:28+10:00 — Duel round-loop logic and per-round result screen (4.16, 4.17)

**Author of prompt:** Mobark Bacran

**AI model:** Claude Sonnet 5

**Prompt / request:**
Work through `docs/task-splits/mobark.md`'s backlog on branch `mobark/feat/duel-round-loop`, building against `src/contracts/roundOutcome.ts` + its mock per the "Building in parallel" section rather than waiting on Tianze/Tanachat's detection code.

**What was done:**

- Added [`src/features/duel/roundLoop.ts`](../../src/features/duel/roundLoop.ts) (ticket 4.17):
  pure best-of-3/5/7 round-loop state — tracks wins per player, decides the match as soon as
  one side reaches a majority, and converts the finished state into a `MatchResult` for the
  `matchResult.ts` contract (Mihir consumes this next).
  False-start losses and tie-round replay rules came from
  `docs/superpowers/specs/2026-09-06-map-duel-implementation-design.md` §10.
  Includes an `assert`-based self-check runnable via `node src/features/duel/roundLoop.ts`
  (no test framework is set up in this repo yet).
- Added [`src/features/duel/RoundResultScreen.tsx`](../../src/features/duel/RoundResultScreen.tsx)
  (ticket 4.16): presentational per-round result screen (winner/tie/false-start + reaction
  times) built from the existing design-system components (`ScreenHeader`, `StatTile`,
  `StatusTag`, `CutCornerButton`).
  Not yet wired into `App.tsx` — there's no duel entry point in the app yet (QR/BLE/countdown
  flow isn't merged), so whoever assembles the full duel screen flow will import it.
- Verified with `npx tsc --noEmit` (clean, aside from pre-existing unrelated `apm_modules/**/*.jsx` errors) and `npx expo export --platform ios` (bundles successfully) — same verification ceiling used for the design-system work, since this repo has no UI test framework yet.
- Branched from `dev` (confirmed in sync with `origin/dev`, user's explicit call despite `docs/task-splits/README.md` flagging `dev` as stale relative to `mobark/receive-branches`).

### 2026-09-10T17:24:00+10:00 — Postmatch summary screen (6.1, 6.2, 6.3)

**Author of prompt:** Mobark Bacran

**AI model:** Claude Sonnet 5

**Prompt / request:**
Continue through the remaining backlog on a new `mobark/feat/postmatch-summary` branch.

**What was done:**

- Refactored [`src/features/duel/roundLoop.ts`](../../src/features/duel/roundLoop.ts): extracted
  the win/false-start tally into a shared `tallyOutcome` helper and exposed `scoreFromRounds`, so
  the summary screen can derive the final score from a `MatchResult`'s round history instead of
  duplicating the tally logic. Committed to `mobark/feat/duel-round-loop` first.
- Branched `mobark/feat/postmatch-summary` off `mobark/feat/duel-round-loop` (not off `dev`) since
  the summary screen needs `scoreFromRounds` — this branch should merge after (or be rebased once)
  `duel-round-loop` lands on `dev`.
- Added [`src/features/postmatch/MatchSummaryScreen.tsx`](../../src/features/postmatch/MatchSummaryScreen.tsx)
  covering all three remaining tickets in one screen: 6.1 (final score, winner, per-round reaction
  times, plus ELO built against `src/contracts/playerStats.ts` + its mock per the "Building in
  parallel" section, since Mihir's 5.4 hasn't landed), 6.2 (a "Rematch" button — always shown; the
  ticket's "particularly after a tied match" reads as motivation, not a gating condition worth
  guessing a threshold for), and 6.3 (a "Back to map" action). Presentational only, same as
  `RoundResultScreen` — no duel entry point exists in `App.tsx` yet to wire it into.
- Verified with `npx tsc --noEmit` (clean, aside from the same pre-existing `apm_modules` errors).
